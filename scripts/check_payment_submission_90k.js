const fs = require('fs');
const path = require('path');
const { createClient } = require('@supabase/supabase-js');
function loadEnv(file){ if(!fs.existsSync(file)) return; for(const line of fs.readFileSync(file,'utf8').split(/\r?\n/)){ const m=line.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/); if(!m) continue; let [,k,v]=m; if ((v.startsWith('"')&&v.endsWith('"'))||(v.startsWith("'")&&v.endsWith("'"))) v=v.slice(1,-1); if(!(k in process.env)) process.env[k]=v; }}
(async()=>{ const root='/Users/uma/Documents/finverno'; loadEnv(path.join(root,'.env')); loadEnv(path.join(root,'.env.local')); const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const investorId='bffc0c5c-3bbf-4c70-9f87-7b25b3c3271c'; const {data: subs, error} = await supabase.from('investor_payment_submissions').select('id,allocation_intent_id,amount,status,payment_date,payment_reference,created_at,allocation_payload,capital_transaction_id').eq('investor_id', investorId).order('created_at',{ascending:false}); if(error) throw error; console.log(JSON.stringify(subs,null,2)); })().catch(err=>{ console.error(JSON.stringify(err,null,2)); process.exit(1);});
